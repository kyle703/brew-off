# 2025 Golden Spoon archive

This is a frozen copy of the original static results portal. Data is baked into `public/snapshot.json` (no live Google Sheets). Built to `/history/2025/` with a hash router so it can sit next to the new app without server rewrites.
